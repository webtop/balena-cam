version: "2"
services:
    balena-cam:
        build: ./balena-cam
        privileged: true
        restart: always
        network_mode: "host"
        ports: [3000]
        labels:
            io.balena.features.kernel-modules: "1"
